let names = {
    "Romio Joliat": 35,
    "Mario Ristrova": 39,
    "Sofia firnando": 50,
  };


function customerAndAge(obj){
    let ls=[]
    let name=`Customer Name : ${names[key]}} `
    let Age =`Age ${name[value]} : `
    ls.push(name)
    ls.push(Age)

 return ls
};


console.log()
/*let recipeInfo = {
    name: "pizza",
    ingredients: ["bread", "tomato", "chicken", "mayo"],
    cookTime: "twoHours",
    price: "25$",
  };/*
  /*function customerAndAge(obj){
    let list=[]
  
    let name = `name: ${obj.name }`
    let ingredients =`ingredients: ${obj.ingredients}`
    let cookTime =`cookTime: ${obj.cookTime}`
    let price =`price: ${obj.price}`
    list.push(name)
    list.push(ingredients)
    list.push(cookTime)
    list.push(price)

  return list
}
console.log(customerAndAge(recipeInfo))*/